{{define "title"}}Home{{end}}
{{define "main"}}
  {{with .Books}}
    {{range .}}
      <div class="book">
        <p>{{.Title}}</p>
        <p>{{.Author}}</p>
        <p>-------</p>
      </div>
    {{end}}
    {{end}}
{{end}}
